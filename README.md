# salary-management-backend

Project scaffold: Node.js + Express + TypeScript + Prisma + SQLite.

Quick start (run these locally):

```bash
npm install
npm run prisma:generate
npm run dev
```

Build and run:

```bash
npm run build
npm start
```

Prisma files: see `prisma/schema.prisma` (SQLite datasource). No models yet.
